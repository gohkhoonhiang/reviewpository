<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#reviewpository-navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">ReviewPository</a>
    </div>
    <div class="collapse navbar-collapse" id="reviewpository-navbar-collapse">
      <% if logged_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "My Items", user_items_path %></li>
            <li><%= link_to "Account Settings", account_settings_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Logout", {:controller => "sessions", :action => "destroy"}, :method => :post %></li>
          </ul>
        </li>
      </ul>
      <% else %>
      <%= form_tag("/login", method: "post", class: "navbar-form navbar-left") do %>
        <div class="form-group">
          <%= text_field_tag(:username, nil, class: "form-control", placeholder: "Username") %>
        </div>
        <div class="form-group">
          <%= password_field_tag(:password, nil, class: "form-control", placeholder: "Password") %>
        </div>
        <%= submit_tag("Login", class: "btn btn-default") %>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/signup">Signup</a></li>
      </ul>
      <% end %>
    </div>
  </div>
</nav>
